services:
  - type: web
    name: itr-bot-backend
    runtime: python
    buildCommand: pip install uv && uv sync --frozen
    startCommand: uv run uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: OPENAI_API_KEY
        sync: false          # set manually in Render dashboard (never commit secrets)
      - key: CORS_ORIGINS
        sync: false          # set to your Vercel URL, e.g. https://itr-bot.vercel.app
      - key: LOG_LEVEL
        value: INFO
      - key: LOG_PRETTY
        value: "false"
      - key: EXTRACTION_CACHE_TTL
        value: "604800"      # 7 days. Cache key is auto-fingerprinted on extraction
                             # source/model changes, so fixes are never served stale.
